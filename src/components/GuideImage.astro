---
export interface Props {
  src: string;
  alt: string;
  caption?: string;
}

const { src, alt, caption } = Astro.props;
---

<div class="guide-image-wrapper">
  <img src={src} alt={alt} class="guide-image">
  {caption && <div class="guide-caption">{caption}</div>}
</div>